
// photo.setAttribute("src", `/assets/images/${array[0].name}/${el}`);
      // photo.setAttribute("class", "lightbox-activate images-photographers");
      // photo.setAttribute("alt", `${el}`);
      // photo.style.cursor = "pointer";

      
{/* <header role="banner">
      <a href="/index.html">
        <img src="assets/images/logo.png" class="logo" />
      </a>
    </header>
    <main id="main-page-photographer">
      <div class="photographer-header" aria-label="Page photographer Profil"></div>
      <form class="filter-form" action="photographer-page.html" method="POST" >
        <label for="filter-select">Trier par</label>
        <select name="filter-select" id="filter-select">
            <option value="popularite">Popularité</option>
            <option value="date">Date</option>
            <option value="titre">Titre</option>
        </select>
    </form>
      <section class="photos-medias" aria-label='photographer works'>
      </section>
    </main> */}